# Development proxy only. Use the VM entry point ending in /api if needed.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
# Production uses the same-origin Caddy route by default.
VITE_API_BASE_URL=/api
